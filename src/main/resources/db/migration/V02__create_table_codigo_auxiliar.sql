CREATE TABLE public.codigo_auxiliar (
    id BIGINT NOT NULL GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    codigo VARCHAR(255) NOT NULL,
    time_geracao_cod TIMESTAMP WITHOUT TIME ZONE NOT NULL
);
